sudo: false

language: go

go:
    - 1.6
    - tip

addons:
    apt:
        packages:
            - libhwloc-dev

install:
    - go get github.com/sirupsen/logrus
    - go get github.com/docker/engine-cli
    - go get gopkg.in/urfave/cli.v2
